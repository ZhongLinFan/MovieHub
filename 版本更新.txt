在进行lbagent的第一个版本也就是支持客户端查询基础服务器和流媒体服务器的版本时，发现其他模块的bug或者其他模块需要增加功能，增加协议等等改动的还是很大的，所以有必要在主目录这里进行全面的索引，当然各个模块的版本也要更新

2023年7月25日09点43分，实现基础服务器开发的部分功能，包括注册，登录，聊天，数据库个人信息同步，一对一聊天，多人聊天等，这期间会更新其他模块的功能，以及解决其他模块的bug
2023年7月31日19点06分，在实现完基础服务器的逻辑伪代码和lb服务器的逻辑伪代码之后，发现需要和数据库那里进行大量的交互，然后就完善了mysql服务器的对每张表的基本操作（之前仅仅是维护了router的表）并进行了每一项基本操作的测试，符合预期。当然了也完善了mysql代理服务器的协议，和mysql数据库的表关系。
由于完善了mysql代理服务器的功能，虽然没有实现baseServe的东西，但是仍然需要打包记录一下，这一版本更新的是数据库代理服务器的核心代码，注意，仍然没有完成2023年7月25日09点43分的大版本，这只是其中一个小版本
2023年7月31日19点27分下一个版本就是完善lb代理服务器和基础服务器的核心功能
2023年8月16日22点14分 完成并测试了lbserver的私聊转发，群组转发，登录反馈同步，同时还解决了一些udp等的一些很重要的bug，可以在lb服务器相关文件看看，不过有些问题还是一头雾水，比如set的比较函数问题，这个解决的云里雾里，根本不知道咋解决的（1-8号写论文了，13-15来学校了），这个版本还是 有必要打个包
2023年8月25日21点34分完成了baseServer的所有功能测试，大致包括15项测试功能，中间解决了mysql和lb服务端的bug，以及baseserver自身的bug问题，并且优化了baseServer注册回调函数时方便传参的问题，以及lb服务器负载排序的问题等等（可以查看对应的bug等）（由于lb服务器更新的地方也很多，所以主目录也打包了lb）
2023年8月26日早上，可以写qt客户端了，主要功能在上一个版本的测试程序中已经包括了。主要是利用qt搭个壳子，今天摸索了一天，打算在linux端写qt程序，这里不再备份客户端源代码，不过bug和版本更新在这里还是要做的
在写客户端的时候，发现客户端，两个服务器的打包解析发送函数都是一样的，后期有时间可以合在一起

2023年9月5日14点01分，qt客户端基础服务部分已经写好，支持群聊私聊，注册，添加，登录下线等基本交互，改进了一个巨大的bug就是关于之前的udp和buffer多线程没加互斥锁的问题，如果不加互斥锁会极度不稳定（可能是上次只简单测了一下看看群聊有没有收到消息，如果消息发多了就会非常混乱不加的话），必须要加，并且改进了udp写buf的一个逻辑（之前是需要撞南墙，这里不在需要了），还有关于一个qt的一个事件循环和自己的事件循环的融合问题也解决了，更加详细的bug见对应的bug目录。不过也可能是加了互斥锁之后，导致现在也不太稳定，比如主要bug记录的没有解决的（反复上下线，中间聊天，会偶尔登录不上等等，很多奇怪的不稳定的小bug，可能需要大量的测试）
2023年9月5日14点09分，必须要改进日志系统，没必要的日志不要打印，关键日志打印出来即可；实现推流服务器（在lb服务器上），实现推流服务，客户端实现电影播放（其中可能包括流媒体地址获取，流媒体服务器负载分配排序，推流，拉流等等）